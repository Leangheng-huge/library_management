A simple C++ Library Management System using SQLite3 to store data permanently. It lets users add, edit, delete, search, and display books through a colorful command-line interface. Future plans include authentication, borrowing features, export options, and a GUI version.
